#include <bits/stdc++.h>
using namespace std;

// We can traverse the whole list from any node
// Insert at begin or end in O(1) by just maintaining one pointer
// Can implement a queue
// Implementing Scheduling algorithms like Round Robin


struct Node{
    int data;
    Node *next;
    Node (int x){
        data = x;
        next = NULL;
    }
};


// While inserting make sure that the last element points to the head

int main(){



    return 0;
}
